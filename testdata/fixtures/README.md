# Test fixtures

Keep fixtures minimal and anonymous. Do not add exports, content, credentials, or configuration copied from a production site.

Use a dedicated fixture directory for each behaviour under test, such as a Hugo project with an invalid link or a generated site with malformed output.
